#!/bin/bash

# window manager
PACKAGES=("${PACKAGES[@]}" "rectangle")

# terminal
CASK=("${CASK[@]}" "iterm2")

# password managers
CASK=("${CASK[@]}" "keepassxc")

# vpn
CASK=("${CASK[@]}" "mullvadvpn")

# browsers
CASK=("${CASK[@]}" "firefox" "google-chrome")

# equipment
CASK=("${CASK[@]}" "logi-options-plus")

# cloud
CASK=("${CASK[@]}" "nextcloud")

# ftp client
CASK=("${CASK[@]}" "cyberduck")

# communication
CASK=("${CASK[@]}" "zoom" "microsoft-teams")

# streaming
CASK=("${CASK[@]}" "obs" "spotify")

# image manipulation
CASK=("${CASK[@]}" "gimp" "krita" "affinity-designer" "affinity-photo" "affinity-publisher")

# document viewers / editors
CASK=("${CASK[@]}" "xournal-plus-plus")
